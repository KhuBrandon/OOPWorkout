class dd{
  int colorOption;
  dd(){
    float xx=random(4);
    if(xx<1) colorOption=0;
    else if(xx<2) colorOption=1;
    else if(xx<3) colorOption=2;
    else colorOption=3;
  }
  void CC(){
    fill(colorOption*50,colorOption*50+20,colorOption*25);
  }
}
  
